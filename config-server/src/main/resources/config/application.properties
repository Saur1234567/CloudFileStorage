# Shared config served to every microservice that pulls from this Config
# Server (spring.config.import=configserver:... in each service). Empty for
# now — add properties here only if you actually want centralized config;
# otherwise every service can keep using its own local application.properties.
